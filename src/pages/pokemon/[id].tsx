import Layout from '@/components/layout';

const DetailPokemon = () => {
	return <Layout title='Detail'></Layout>;
};

export const getServerSideProps = async () => {};

export default DetailPokemon;
